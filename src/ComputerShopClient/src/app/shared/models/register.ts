import { PhoneNumber } from './phoneNumber';

export class Register {
    firstName: string;
    lastName: string;
    userName: string;
    email: string;
    password: string;
    phoneNumber: PhoneNumber;
}